/**
 * @file
 * scripts for show/hide selectbox
 * @param {type} $
 * @param {type} Drupal
 * @param {type} window
 * @param {type} document
 * @param {type} undefined
 * @returns {undefined}
 */
(function($, Drupal, window, document, undefined) {
    $(document).ready(function() {
      // $("#edit-delivery-delivery-pane-field-display-datepicker-popup-0").datepicker({ beforeShowDay: $.datepicker.noWeekends })
    });

})(jQuery, Drupal, this, this.document);

